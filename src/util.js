import { EVENT_TYPES } from './constants';

const getRandomArrayElement = (array) => Math.floor(Math.random() * array.length);

const getRandomEvent = () => {

};

export {getRandomArrayElement, getRandomEvent};

